next(null, {
    code: '0000',
    data: {
        /**
         * 服务使用占比比例数据
         * 服务名称:数量
         */
        serviceRates: [
            /**
             * 服务名称: name
             */
            /**
             * 服务使用数量:value
             */
            { value: Math.ceil(Math.random() * 100), name: '服务A' },
            { value: Math.ceil(Math.random() * 1000), name: '服务B' },
            { value: Math.ceil(Math.random() * 100), name: '服务C' },
            { value: Math.ceil(Math.random() * 1000), name: '服务D' },
            { value: Math.ceil(Math.random() * 100), name: '服务E' },
            { value: Math.ceil(Math.random() * 1000), name: '服务F' },
            { value: Math.ceil(Math.random() * 100), name: '服务G' },
            { value: Math.ceil(Math.random() * 1000), name: '服务H' },
            { value: Math.ceil(Math.random() * 10), name: '服务I' },
            // {"value": Math.ceil(Math.random()*1000),  "name": "身份证实名实人有效期认证"},
            // {"value": Math.ceil(Math.random()*1000),  "name": "电信手机三要素认证"},
            // {"value": Math.ceil(Math.random()*1000), "name": "联通手机三要素认证"},
            // {"value": Math.ceil(Math.random()*1000),  "name": "移动手机三要素认证"},
            // {"value": Math.ceil(Math.random()*1000), "name": "银行卡三要素认证"}
        ],
        /**
         * 总开始时间
         */
        totalBeginDate: 1590940800000,
        /**
         * 总结束时间
         */
        totalEndDate: 1620403199141,
    },
})
